server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: customer-service
          uri: lb://CUSTOMER-SERVICE      # must match Eureka service name
          predicates:
            - Path=/api/customers/**      # forward all /api/customers/** requests
          # no StripPrefix here, keep full path

        - id: order-service
          uri: lb://ORDER-SERVICE
          predicates:
            - Path=/api/orders/**          # forward all /api/orders/** requests
          # no StripPrefix

        - id: config-server
          uri: lb://CONFIG-SERVER
          predicates:
            - Path=/api/config/**          # forward config requests
          # no StripPrefix

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/   # use container name
  instance:
    prefer-ip-address: true
    instance-id: config-server:${server.port}
jwt:
  secret: mysecretkeyformicroservices1234567890
  expiration: 3600000  # 1 hour