cd d:

cd d:\Old\ System/Backups/SOW5/Springboot/preparation/ECommerceApplication/ECommerceAppl/

git clone https://github.com/YembuluruVenkatesh/ECommerceAppl.git

git add .

git commit -m "Second Commit"

git push origin

git pull


git checkout -b productServiceAdded 

git add .
git commit -m "Product Service is Added"

git push -u origin productServiceAdded

-------------------------



mvn clean install

mvn spring-boot:run

------------------------


docker run --name mysql-db -e MYSQL_ROOT_PASSWORD=root -e MYSQL_DATABASE=ecommerce -p 3307:3306 -d mysql:8

-----------------

 Run Zipkin Server

 docker run -d -p 9411:9411 openzipkin/zipkin

 run:
 java -jar zipkin-server-<version>.jar

--------------------

CustomerService and OrderService are independent microservices.
Both services are registered with Eureka Discovery Server.
Configuration is centralized using Spring Cloud Config Server.
Requests route through the API Gateway.
Configuration is dynamically refreshable using @RefreshScope.
Building Resilient Microservices (Circuit Breaker, Retry & Fallback)
we done till now, now we want to added the security JWT tokens 


